language: cpp
script: mkdir build ; cd build ; CXXFLAGS="-std=c++11 -g -Wall" cmake ../ ; make ; ./test-runner --on-failure ./dump-backtrace
compiler:
    - g++-4.7
before_script:
    - sudo add-apt-repository --yes ppa:ubuntu-toolchain-r/test  
    - sudo apt-get update  
    - sudo apt-get install g++-4.7
    - export CXX=g++-4.7
    - cd test
